version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "80:80"
    depends_on:
      - builder

  builder:
    build:
      context: .
      dockerfile: Dockerfile.build
    volumes:
      - .:/geoserver
    command: mvn package -DskipTests -Dmaven.artifact.threads=1
    working_dir: /geoserver/src


